import React, { useState, useEffect } from "react";
import Cookies from "js-cookie";

/**
 * 
 * @param chatterName Name of the person the client is chatting with
 * @param chatterUUID UUID of the person the client is chatting with
 * @param chatSegment A segment of the chat history returned from the server 
 */
export default function Chat({chatterName, chatterUUID, chatSegment}) {

}